这是整个系统的自然语言处理模块的1.1版本，也是最初版本。
目前配合处理器进行学习的语料来源网站只有同花顺，更多的内容将会在下次更新进行（当然这也很简单）。
当前语言处理的核心算法是修正TextRank后的CenterRank,与其在同一py文件中的OriginTextRank没有经过测试，可能无法使用，其余函数皆通过测试且运行达到了预期。
在CenterRank的基础上我利用动名词对匹配，来寻找所有的可能的组合，并在未来大量的学习中不断筛选出最合理的组合，筛去一些由于算法本身缺陷而产生的奇怪组合，从而通过构建动名词与名动词对的方式来达到语语意分析的目的。
